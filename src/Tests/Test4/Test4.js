const Test4 = () => {

    const days = ['Mo', 'Tu', 'We', 'Th', 'Fr', 'Sa', 'Su'];

    const getDay = ({ day, number }) => {
        // It must return the day of the week depending on the number indicated, for example,
        //if I say Th it must start from Thursday and the number is 10, it must return Sa (Saturday)

        return {};
    }

    return {
        days,
        getDay
    }
}

export default Test4
